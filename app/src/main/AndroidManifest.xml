<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vasmajom.tars">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">
        <activity
            android:name=".activities.PayeeList"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.AddPayee"
            android:theme="@style/AppTheme.NoActionBar"
            android:parentActivityName=".activities.PayeeList">
        </activity>
        <activity
            android:name=".activities.WalletList"
            android:theme="@style/AppTheme"
            android:parentActivityName=".activities.PayeeList">
        </activity>
        <activity
            android:name=".activities.AddWallet"
            android:theme="@style/AppTheme.NoActionBar"
            android:parentActivityName=".activities.WalletList">
        </activity>
        <activity
            android:name=".activities.Contact"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity
            android:name=".activities.Reset"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
    </application>

</manifest>